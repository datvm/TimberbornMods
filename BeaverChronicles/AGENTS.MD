# Agent Instructions

Before working on this project, read `UsefulAgentContext.MD` in this folder.

While working, keep an eye out for context that would help a future AI agent continue safely: architecture notes, project conventions, non-obvious dependencies, known sharp edges, and verification commands that are useful for this project.

When you learn something durable and relevant, add or update it in `UsefulAgentContext.MD`. Keep entries concise, factual, and project-specific. Do not include temporary command output, private secrets, speculation, or unrelated conversation notes.

Follow the existing code style in the repository. In particular:

- Use braces for all conditionals and loops, even single-line bodies.
- Prefer current C# features already used in the project, such as collection expressions, pattern matching, and primary constructors.
- Keep changes scoped to the requested project behavior.
- Do not revert unrelated user changes in the working tree.
